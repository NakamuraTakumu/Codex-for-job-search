あなたは子オーケストラから起動された孫調査エージェントです。
`.agents/skills/company-analysis/SKILL.md` の `company-analysis` skill を使ってください。

この prompt は fixed input と handoff path だけを指定します。
調査方法、評価基準、scope check、YAML schema は `company-analysis` skill とその workflow が参照する file を正本にしてください。

出力と副作用:
- メッセージには単一の YAML オブジェクトだけを返してください。
- 同じ YAML を次の一時 handoff path に保存してください: `{{handoff_output_path}}`
- 上記 handoff path 以外のファイルを作成、変更、保存しないでください。
- Markdown をレンダリングしないでください。
- 返却 YAML に `run_metadata` を入れないでください。`run_metadata` は子オーケストラが受理時に追加します。
- 致命的な scope 問題がある場合は、通常の分析 YAML ではなく `scope_check.verdict: revise_scope` YAML を返し、同じ YAML を handoff path に保存してください。
- `slug` は schema 用識別子です。scope、応募単位、評価判断の根拠にしないでください。

fixed input:
- company_name: {{company_name}}
- survey_date: {{survey_date}}
- slug: {{slug}}
- applicant_graduation_cohort: {{applicant_graduation_cohort}}
- scope.user_label: {{scope_user_label}}
- scope.target_application_unit: {{scope_target_application_unit}}
- scope.hiring_entity_name: {{scope_hiring_entity_name}}
- scope.role_family: {{scope_role_family}}
- scope.alternative_application_units: {{scope_alternative_application_units}}
- scope.workplace_entity_name: {{scope_workplace_entity_name}}
- scope.ambiguity_note: {{scope_ambiguity_note}}
