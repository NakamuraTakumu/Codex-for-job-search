`.agents/skills/company-analysis/SKILL.md` の `company-analysis` skill を使ってください。

メッセージには、単一の YAML オブジェクトだけを返してください。
次の一時 handoff path にも、メッセージで返す YAML と同一内容を保存してください: `{{handoff_output_path}}`
handoff path のファイル名は親が割り当てた UUID です。slug は YAML 内の `slug` にだけ入れてください。
この handoff path 以外のファイルを作成、変更、保存しないでください。
`report/`、`document/`、その他 production/test artifact path 配下に書き込まないでください。
Markdown をレンダリングしないでください。
最初に固定 scope が分析可能か確認してください。致命的な scope 問題がある場合は、通常の分析 YAML を作らず、`company-analysis` skill の `scope_check.verdict: revise_scope` schema だけを返してください。
`scope_check.verdict: revise_scope` の場合も、返す YAML と同一内容を上記 handoff path に保存してください。

現在の分析対象:
- このプロンプトは自己完結した指示として扱ってください。
- 以前の分析 payload、以前の slug、以前の finding、以前の turn の会社情報はすべて無視してください。
- 現在意図している slug は正確に `{{slug}}` です。
- 現在の固定 scope と、この対象についてあなたが見つけた source だけから YAML を判断・記入してください。

固定 scope:
- company_name: {{company_name}}
- survey_date: {{survey_date}}
- slug: {{slug}}
- scope.user_label: {{scope_user_label}}
- scope.target_application_unit: {{scope_target_application_unit}}
- scope.hiring_entity_name: {{scope_hiring_entity_name}}
- scope.role_family (`researcher` / `research_engineer` / `engineer` / `consultant` / `generalist` / `other` のいずれか。採用年度、cohort、採用区分ではありません): {{scope_role_family}}
- scope.alternative_application_units (同じ hiring_entity_name 配下の他の採用ルート、職種トラック、応募単位): {{scope_alternative_application_units}}
- scope.stability_entity_name: {{scope_stability_entity_name}}
