interface:
  display_name: "Company Analysis Runner"
  short_description: "Launch company-analysis child orchestrators in parallel"
  default_prompt: "Use $company-analysis-runner to pass company names and requested roles to child orchestrators, then summarize their result YAML."
